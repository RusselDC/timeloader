export type LoginContextType = {
    email: string,
    password: string,
    contact_number: string,
    institution_id : number,
    trainee_id : number,
    first_name: string,
    last_name: string,
    birth_date: string,
    gender : string,
} | null